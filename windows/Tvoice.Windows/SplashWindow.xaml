<Window x:Class="Tvoice.Windows.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Tvoice.Windows.Controls"
        Title="Tvoice"
        Width="520"
        Height="340"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Margin="24" Padding="28,24" CornerRadius="22"
            Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
            Effect="{StaticResource FloatingShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Ellipse Width="7" Height="7" Fill="{DynamicResource AccentBrush}" Margin="0,0,9,0" />
                <TextBlock Text="TOJIKTELECOM" FontSize="11" FontWeight="SemiBold"
                           Foreground="{DynamicResource MutedTextBrush}" />
            </StackPanel>

            <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <controls:BrandMark Width="76" Height="76" HorizontalAlignment="Center" />
                <TextBlock Text="Tvoice" Margin="0,15,0,0" FontSize="38" FontWeight="SemiBold"
                           HorizontalAlignment="Center" />
                <TextBlock Text="Звонки, сообщения и видео" Margin="0,6,0,0" FontSize="12"
                           Foreground="{DynamicResource MutedTextBrush}" HorizontalAlignment="Center" />
            </StackPanel>

            <ProgressBar Grid.Row="2" Width="96" Height="3" IsIndeterminate="True"
                         Foreground="{DynamicResource AccentBrush}" Background="{DynamicResource SurfaceAltBrush}" />
        </Grid>
    </Border>
</Window>
